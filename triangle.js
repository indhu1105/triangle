let height = +prompt("enter the height",0);
let breadth = +prompt("enter the breadth",0);
let result;

function area()
{
   result = (height * breadth) / 2;
   console.log(result);
   return result;
}